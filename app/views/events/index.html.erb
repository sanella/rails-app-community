<h1>Events</h1>
<ul>
<% @events.each do |event| %>
    <li><%= event.name %>, <%= event.date %>,
      <%= link_to 'Edit', edit_event_path(event) %>,
      <%= link_to 'Read more', event_path(event) %>,
    </li>
<% end %>
</ul>
<p>Add new event</p> <%= link_to 'Add event', new_event_path %>